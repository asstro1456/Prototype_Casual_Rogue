# 룬 트레이스 패치노트

현재 개발판 `룬 트레이스`의 변경 내역을 기록한다.

- 기록 범위: `rune-trace/`의 `v0.0.1` 이후
- 현재 기준 버전: `v0.0.8`
- 이전 프로토타입 아카이브는 기록 범위에서 제외한다.
- 인수인계 문서와 Git 변경 내역에서 사용자 결정으로 확인되는 내용은 첫 번째 섹션에 기록한다.
- 구체적인 수치·알고리즘·예외 처리처럼 구현 과정에서 Codex가 판단하거나 보강한 내용은 두 번째 섹션에 기록한다.
- 이번 패치노트 문서 추가는 게임 버전을 올리지 않는다.

## 1. 사용자가 구현을 요청한 내용

### v0.0.8

- 전투 능력 `유혹`, `연속 격파`, `끝점 참격`을 레벨업 선택지로 교체하고 같은 능력을 다시 선택하면 최대 3단계까지 강화되도록 했다.
- 룬 확정 전에 직접 처치 대상과 능력 추가 처치 대상을 계산하고, 처치 예정 몬스터의 투명도를 낮춰 실시간으로 표시했다.
- 룬 경로로 직접 처치한 몬스터만 EXP를 주고 능력 추가 처치는 EXP 없이 플로어 처치 수에 포함하도록 분리했다.
- `유혹`은 새 룬 때문에 이동 목적지가 막히는 생존 몬스터를 경로 진행 순서와 화면 위치 우선순위에 따라 단계별 1체·2체·전체 추가 처치하도록 했다.
- `연속 격파`는 한 룬으로 일반 몬스터 2체 이상을 직접 처치하면 플로어당 처음 한 번, 룬 끝점과 가까운 몬스터를 단계 수만큼 추가 처치하도록 했다.
- `연속 격파`의 거리는 `dx² + dy²`로 비교하고 동률이면 위쪽, 그다음 왼쪽 몬스터를 우선하도록 했다.
- `끝점 참격` 1단계는 끝점 진행 방향 앞 1칸, 2단계는 직교 방향 앞 3칸, 3단계는 끝점과 직전 룬 칸을 제외한 주변 7칸을 공격하도록 했다.
- 유효한 끝점이 완성되면 `끝점 참격` 범위를 테두리로 표시하고 경로 변경 시 즉시 갱신하도록 했다.
- 되돌리기와 플로어 초기화가 능력 단계와 `연속 격파`의 플로어당 발동 상태까지 복원하도록 했다.
- 미확정인 복합 능력 처리와 대각선 종료 룬의 `끝점 참격` 2단계 배치는 연결하지 않았다.

### v0.0.7

- 턴 시작 시 몬스터의 다음 이동 목적지를 예약하고 몸체 안에 방향 화살표를 표시했다.
- 이동 가능한 목적지가 없는 몬스터에는 정지 표시를 적용했다.
- 새 룬 흔적이 예약 목적지를 막으면 재탐색하지 않고 정지하도록 했다.
- 되돌리기와 플로어 초기화가 저장된 이동 의도까지 복원하도록 했다.
- 레벨업 요구치를 초기 테스트값인 고정 EXP 7로 변경하고 초과 EXP 이월을 유지했다.
- 레벨업 모달에 `보드 보기`를 추가하고, 보드에서 `능력 선택 보기`로 돌아올 수 있게 했다.
- 능력을 선택하기 전에는 다음 턴으로 진행하거나 선택을 생략할 수 없게 했다.
- 한 챕터를 3스테이지×3플로어로 구성하고 진행 HUD에 각 스테이지를 별도 행으로 표시했다.
- 스테이지 행 사이에 30px 간격을 적용해 서로 다른 구간임을 구분했다.
- 챕터 마지막 플로어가 끝나면 대기 중인 레벨업 선택을 생략하도록 했다.
- 정지 의도를 점 기호 대신 사용자가 제공한 자물쇠 이미지로 표시했다.
- `챕터 재도전`은 스테이지 3의 마지막 플로어를 완료한 챕터 종료 화면에서만 제공하도록 제한했다.
- 스테이지 1·2에서는 `스테이지 재도전`으로 해당 스테이지를 처음부터 다시 시작할 수 있게 했다.
- 정지 의도 자물쇠 이미지를 투명 배경 PNG로 교체했다.

### v0.0.6

- 개발명을 `프리즘 리콜`에서 `룬 트레이스`로 변경했다.
- 현재 개발판에서 `프리즘 리콜` 명칭을 제거하고, 이전 아카이브에만 남겼다.
- 게임 헤더, 모든 모달, 허브, README, CSS·JavaScript 캐시 쿼리의 버전 표기를 동기화했다.
- 전역 및 프로젝트 인수인계 문서를 작성했다.
- 요청에 따라 이 패치노트 문서를 추가하되 게임 버전은 유지했다.

### v0.0.5

- 적 처치로 누적한 EXP가 3에 도달할 때마다 레벨이 오르도록 했다.
- 레벨업 시 `능력 1`, `능력 2`, `능력 3` 중 하나를 선택하는 모달을 추가했다.
- 아직 효과가 정해지지 않은 능력은 획득 기록만 남기도록 했다.
- `한 수 되돌리기`로 마지막 룬 완성 전의 적 위치, 오염, EXP, 레벨, 획득 능력을 복원하도록 했다.
- `층 초기화`로 현재 층의 최초 배치를 복원하도록 했다.

### v0.0.4

- 왼쪽 룬 목록의 미리보기가 작거나 형태를 구분하기 어려운 문제를 개선했다.
- 룬의 시작점과 끝점을 미리보기에서 구분할 수 있게 표시했다.

### v0.0.3

- 룬을 미리 선택하지 않고 그린 경로를 남은 룬 목록과 자동으로 대조하도록 변경했다.
- 룬 목록을 게임판 왼쪽의 세로 목록으로 배치했다.
- 적 1체 처치 시 EXP 1을 획득하고 다음 층까지 누적하도록 했다.
- 허브에서 현재 개발판은 크게 표시하고 이전 프로토타입은 하단의 작은 아카이브 목록으로 정리했다.

### v0.0.2

- 대각선 룬을 그릴 때 포인터가 중간의 가로·세로 칸을 스쳐도 대각선 입력을 우선하도록 조작을 보정했다.

### v0.0.1

- 기존 전사·궁수 전투 및 실시간 디펜스에서 룬 경로 그리기 기반 프로토타입으로 전환했다.
- 한 층에 무작위 룬 4개를 제시하고 모두 완성하면 층을 통과하도록 했다.
- 룬을 회전하거나 좌우 반전해서 그려도 인정하도록 했다.
- 완성한 룬 경로 위의 적을 처치하도록 했다.
- 살아남은 적이 이동하고 빈 칸을 오염시키도록 했다.
- 남은 룬을 놓을 공간이 없으면 실패하도록 했다.
- 이전 프로토타입을 삭제하지 않고 독립 실행 가능한 아카이브로 보존했다.
- 현재 개발판과 아카이브로 이동할 수 있는 프로토타입 허브를 추가했다.

## 2. Codex가 추측하거나 판정이 애매해 보강한 내용

### v0.0.8

- 추정한 내용: 서로 다른 능력을 둘 이상 보유하면 모든 전투 능력의 효과와 미리보기를 멈추고 `복합 발동 보류`로 표시했다. 추정 이유: 능력 처리 순서와 대상 중복 규칙이 미정인 상태에서 임의의 복합 결과를 만들지 않기 위해서다. 기획 확인 필요: 복합 보유 시 처리 순서, 중복 대상 제거, 각 능력의 조건 판정 기준.
- 추정한 내용: `연속 격파`의 남은 일반 몬스터가 능력 단계보다 적으면 발동하지 않고 플로어당 발동 상태도 소비하지 않는다. 추정 이유: 문서에서 해당 상황의 발동권 소모가 보류되어 있어 미확정 분기를 실행하지 않도록 막았다. 기획 확인 필요: 대상 부족·0체 상황의 부분 처치 여부와 발동권 소비 여부.
- 추정한 내용: 최대 3단계 능력을 다시 선택하면 단계를 올리지 않고 해당 레벨업 선택만 완료한다. 추정 이유: 모든 선택지가 최대 단계일 때 진행이 막히는 상태를 피하기 위해서다. 기획 확인 필요: 최대 단계 이후 레벨업 선택 규칙과 대체 보상.
- 추정한 내용: `유혹`의 같은 열 우선순위에서 룬 시작점과 끝점의 세로 진행 방향을 사용하고, 시작·끝의 행이 같으면 마지막 구간의 세로 방향을 사용했다. 추정 이유: 문서의 `룬 진행 방향 쪽`을 7×7 화면 좌표에서 항상 같은 결과가 나오도록 변환하기 위해서다. 기획 확인 필요: 굽거나 시작·끝 높이가 같은 룬의 정확한 우선 방향.
- 현재 프로토타입에는 보스가 없어 `연속 격파`의 보스 제외와 `끝점 참격`의 보스 최대 1회 유효 타격은 실제 대상에 연결하지 않았다. 보스 기본형이 확정되면 대상 유형과 타격 횟수 판정을 추가해야 한다.

### v0.0.7

- 보스 기본형과 변형이 확정되지 않아 각 스테이지의 3번째 플로어도 기존 일반 보드 규칙을 사용하는 진행 구조 테스트로 구현했다.
- 여러 몬스터가 같은 목적지를 예약하지 않도록 매 턴 몬스터 순서를 무작위로 섞은 뒤 예약 우선순위를 부여했다.
- 제공된 자물쇠 원본은 투명 채널 없이 체크무늬가 포함된 RGB 이미지여서, 형태를 유지한 크로마키 이미지를 생성한 뒤 배경을 알파 채널로 제거했다.
- `스테이지 재도전`은 해당 스테이지 첫 플로어 진입 시점의 EXP·레벨·능력을 복원하고 첫 플로어의 무작위 배치를 다시 생성하도록 구현했다.
- 스테이지 1·2 완료 모달에는 `스테이지 재도전`과 현재 성장 상태를 유지하는 `다음 스테이지`를 제공했다.
- 결과 모달을 닫아 진행 수단이 사라지는 상태를 막기 위해 플로어·스테이지·챕터 결과와 실패 모달은 결과 버튼으로만 닫히도록 보강했다.
- 저장 기능은 아직 보류 상태이므로 스테이지 중단 후 재접속 복귀는 구현하지 않았다.
- 마지막 룬 완성 시 생존 몬스터의 이동·오염을 생략하는 기존 동작은 변경하지 않았다. 최종 턴 처리 기준은 추후 확인이 필요하다.

### v0.0.6

- 게임 규칙이나 수치에 대한 추가 판단은 없었다.
- 이름 변경 범위를 현재 개발판과 문서로 한정하고, 기존 아카이브 파일은 수정하지 않았다.

### v0.0.5

- 한 번의 룬으로 여러 레벨을 넘으면 능력 선택 모달을 레벨 순서대로 처리한 뒤 클리어·실패 결과 모달을 표시하도록 대기열을 구성했다.
- 레벨업 모달을 닫으면 해당 레벨의 능력 선택을 건너뛴 것으로 처리했다.
- 같은 능력을 여러 번 선택할 수 있게 두고, 획득 목록에는 중첩 횟수를 표시했다.
- 되돌리기와 층 초기화를 위해 룬, 적, 오염, EXP, 레벨, 능력, 결과 대기 상태를 함께 저장하는 스냅샷 방식을 사용했다.
- 잘못된 상태에서 조작되지 않도록 되돌릴 기록이 없을 때 `한 수 되돌리기` 버튼을 비활성화했다.

### v0.0.4

- 서로 다른 비율의 룬을 동일한 48×48 좌표 영역에 맞춰 확대·중앙 정렬했다.
- 미리보기 선 굵기, 노드 크기, 목록 폭과 카드 높이를 가독성 기준으로 조정했다.

### v0.0.3

- 그리는 도중 남은 룬들의 경로 접두부와 일치하는지 비교해 자동 인식 후보를 좁히도록 했다.
- 같은 층을 다시 시작할 때 해당 층에서 획득한 EXP를 층 시작 시점으로 복원하도록 했다.
- 자동 룬 인식과 충돌하지 않도록 기존 룬 선택 상태와 선택 버튼 동작을 제거했다.
- 현재판과 아카이브의 시각적 위계를 만들기 위해 허브 카드 크기와 배치를 구체화했다.

### v0.0.2

- 대각선 후보 방향과 포인터 이동량을 비교하는 스냅 판정을 추가했다.
- 대각선만 가능한 경로에서 가로·세로 칸이 먼저 입력되지 않도록 제한했다.
- 잘못 들어간 직교 칸 다음에 대각선 칸이 입력되면 유효한 룬 경로에 한해 직교 칸을 대체하도록 보정했다.
- 대각선 스냅 임계값은 구현용 임시 수치로 정했다.

### v0.0.1

- 게임판 크기를 7×7로 정했다.
- 룬 템플릿을 `일섬`, `굽은 뿔`, `별의 사선`, `서약`, `여우걸음`, `쌍봉` 6종으로 구성하고 각 경로 길이를 정했다.
- 층이 올라갈수록 적 수가 증가하되 최대 12체가 되도록 임시 공식을 적용했다.
- 생존 적이 이동한 뒤 한 차례에 최대 3칸을 오염시키도록 임시 수치를 적용했다.
- 적 이동 방향, 오염 대상 선택, 룬 색상과 명칭, 피드백 문구, 결과 모달의 세부 표현을 프로토타입용으로 구체화했다.
- 외부 이미지·폰트·라이브러리 없이 실행할 수 있도록 CSS 기반 더미 그래픽과 단일 페이지 구조로 제작했다.
